package web.jelton.musicgen.generator.Enum;

/*
 * Represents the role in the song which the track plays
 * Used to determine how notes are generated and the midi instrument voicing
 */
public enum TrackType {
    Chord,
    Melody,
    Bass
}
